# 阶段 9B：手势翻页实机验收

当前状态：识别状态机和阶段 8 翻页接口已集成、编译通过，待实机验证。适用范围为左手佩戴、表盘朝外；电脑端程序与无线协议未修改。

## 操作流程

1. 在 VS Code 烧录当前固件，不擦除 Flash。关闭串口监视器后，按阶段 8 的方法连接电脑并打开 WPS/PowerPoint 全屏放映，在电脑端启用演示控制。
2. 手表进入“演示遥控”。短按拨轮一次启用手势：
   - `手势识别  等待`：传感器在运行，但尚未满足胸前准备姿态。
   - `手势识别  已开启`：胸前姿态已连续稳定，可以横挥。
   - `手势识别  暂未启用`：功能关闭；短按可重新开启。
   - 若显示 `BMI270 error`，停止测试并重启手表。
3. 手臂自然放下，然后抬到胸前并保持表盘朝外。等文字变为“已开启”后，再快速向自己的左或右横挥，在终点停住，随后直接放下手臂。
4. 左挥应发送上一页，右挥应发送下一页。一次完整动作最多发送一次；终点刹停和放手不能触发反向翻页。再次抬手并等“已开启”后才能做下一次。

## 当前识别条件

- 胸前准备姿态连续稳定至少约 0.15 秒；离开准备姿态后只保留约 0.45 秒启动窗口。
- 陀螺仪原生 Y 轴决定方向；方向角速度峰值至少 240°/s。
- 同一次动作中，超过 100°/s 部分累计至少约 12°，作为最低动作幅度门槛；这不是厘米距离。
- 动作跟踪最长 0.9 秒；触发后锁定，离开准备姿态约 0.5 秒后才回到等待准备状态。
- 手势事件复用阶段 8 的 `watch_host_link_send_presentation_action()`：断线、电脑端禁用或队列忙时按原逻辑丢弃，不重放。

## 实测顺序

先确认页面状态切换，再逐项记录结果：

1. 规范左挥 10 次、规范右挥 10 次，记录正确次数、漏识别、反向识别和是否重复翻页。
2. 快速短幅、慢速全幅各左右 5 次，都不应翻页。
3. 只抬手放下、自然转腕看表各 10 次，都不应翻页。
4. 手势开启后自然讲话比划 1 分钟、原地走动或自然摆臂 1 分钟，记录误触次数。
5. 横挥后终点停住再放手，确认放手不触发反向翻页；连续两次动作之间必须重新放下、抬起并等待“已开启”。
6. 手势开启时，上拨/下拨仍能稳定翻页；短按关闭后所有手势无效，拨轮仍有效。
7. 断开电脑后横挥，确认不补发；重连后必须重新做动作。长按退出应停止采样，KEY4 应按原逻辑息屏并关闭手势，唤醒后手势保持关闭。
8. 退出演示遥控后检查指南针调平和抬腕唤醒，确认 BMI270 原配置已恢复。

第一轮可先只做左、右各 3 次及一次短按关闭测试，把页面状态和翻页结果反馈回来；发现方向相反、一直无法就绪、误触或漏识别时先停止批量测试，再调整映射或阈值。
