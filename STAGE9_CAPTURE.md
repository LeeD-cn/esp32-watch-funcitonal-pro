# 阶段 9A：独立六轴采集

当前状态：采集代码已实现，实机采集待验证。此步骤只收集数据，尚未实现手势翻页。

## 方案与边界

- 固定左手佩戴、表盘朝外。左右方向指佩戴者自己的左右；保持每次相近的准备姿态。
- 复用 BMI270 和现有 USB 命令入口；独立 Python 工具保存 CSV。现有 Qt 上位机和无线协议不变，不需要打开上位机。
- 临时启用加速度计和陀螺仪，配置 100 Hz、±4g、±1000°/s。数据先存 RAM，采完恢复原寄存器配置，再通过 USB 导出，不写 NVS。
- 每次录制 1～15 秒；默认准备 3 秒、录制 6 秒。采样期间暂停其他加速度读取，保持亮屏；KEY4 仍可取消采集后息屏或关机。
- USB 线留足松弛余量，避免限制动作。第一版采用寄存器轮询，实际节奏以 CSV 时间戳和质量报告为准；若间隔不稳定，先修采集，再调识别算法。

## 第一次：只采一段静止数据

1. 在 VS Code 烧录当前构建，确认端口属于手表；不用擦除 Flash。烧录后关闭串口监视器，以及上位机的 USB 连接。
2. 唤醒手表，停在主表盘。左手戴好，抬到准备挥动的位置，表盘朝外，保持不动。
3. 在项目根目录的 **ESP-IDF 终端**运行：

```powershell
python tools/capture_imu.py --label still --seconds 6
```

工具会列出串口，输入手表当前端口（此前是 COM18，但应重新核对）。也可用 `--port COM18` 指定。

若普通 PowerShell 找不到依赖，使用已安装的 ESP-IDF Python，无需另装工具：

```powershell
& 'D:\Espressif\python_env\idf5.2_py3.11_env\Scripts\python.exe' tools/capture_imu.py --label still --seconds 6
```

4. 按 Enter，准备后听到提示音开始记录，整个过程保持不动。第二次提示音表示记录结束，等待显示保存路径。
5. 输出位于根目录 `captures/`，每组包含 `.csv` 和 `.json`。JSON 记录采样间隔、量程、状态和质量提示。失败数据保留为 `.partial.csv`，不要拿来调阈值。
6. 把保存路径和工具输出反馈到当前对话，先分析这一组。此时不用一次采完全部动作。

## 静止数据确认后：动作样本

建议先试左右各 3 次确认动作约定，再正式各 20 次。每次 6 秒只做一个完整动作：手臂先自然放下；提示音后抬手到胸前，短暂停稳；向目标方向快速横向挥动；在终点停住；随后直接放下手臂，不横向挥回胸前。必须把抬手、主动作、终点停止和放手的全过程都录下来。

建议节奏：开始后约 0～0.8 秒抬到胸前，停稳约 0.3～0.5 秒，横向挥动约 0.2～0.6 秒，终点停约 0.2～0.4 秒，然后放下手并保持静止。手表不能判断是否真的位于胸前，只用姿态、停稳和运动过程判断准备状态。

```powershell
python tools/capture_imu.py --label left --trials 20
python tools/capture_imu.py --label right --trials 20
```

每次需要按 Enter 单独开始；出现质量提示会暂停本批。另采干扰动作，每类先 5 组，每组 6～10 秒：

| 标签 | 动作 |
|---|---|
| still | 准备姿态静止 |
| raise / lower | 普通抬腕 / 放下手臂 |
| rotate | 正常转腕看表 |
| talk | 讲话时自然比划，避免故意模仿标准挥手 |
| walk | 原地踏步或 USB 线允许范围内的自然走动 |
| slow_left / slow_right | 横向距离正常但故意缓慢的无效挥动，用于确定最低速度 |
| short_left / short_right | 速度较快但横向距离很短的无效挥动，用于确定最低动作幅度 |

例如 `python tools/capture_imu.py --label talk --seconds 10 --trials 5`。不能靠线牵住手腕，也不要为了采样拉扯电脑。

## 数据解释与完整性

- CSV 保存原始六轴、换算后的 mg 和 °/s、采样序号、ESP 单调时间戳和 BMI270 计数器。
- 当前已有轴向依据：屏幕向右 X+、向上 Y+、向外 Z+；仍需实测确认戴在手上后的动作映射。静止时加速度向量模长应约 1000 mg，陀螺仪应接近 0，不能要求每个加速度轴都为 0。
- `timestamp_us` 是 I2C 数据读取前后时间的中点，不是硬件锁存时间。`sensor_ticks` 是可回绕的 24 位自由运行计数器，不是逐样本时间戳，不以其推算精确丢样数。
- BMI270 不能直接可靠测量手移动了多少厘米。首版把“最低距离”实现为动作幅度门槛：方向轴必须达到最低峰值速度，并在同一次连续动作内积累足够的同向角度/横向运动量。快而短和远而慢都作为无效样本验证，不把该结果显示为厘米。
- 工具验证序号连续、时间递增、帧数量和设备结束状态。完整传输不等于动作有效；有效采样率、长间隔、饱和警告用于人工检查，尚不判定动作方向。
- 不完整、取消、传感器错误或恢复错误都不得当作成功样本。USB 拔出时本组无法补传；设备仍会结束有限时长的采集并尝试恢复。
- 电脑 Ctrl+C 不发送取消命令：等待该组时限结束后再试，或按 KEY4 取消。采集中不要操作其他页面；采样命令会暂时占用 USB 配置任务。

寄存器与换算依据：[Bosch BMI270 SensorAPI 定义](https://github.com/boschsensortec/BMI270_SensorAPI/blob/master/bmi2_defs.h)。采集目录已加入 `.gitignore`。

## 实机验收清单与下一步

- 静止数据六轴可信、采样间隔合理；连续采集多组无异常。
- 分别在准备、记录和导出期间按 KEY4，确认能取消并息屏，唤醒后能重新采集。
- 采集后指南针调平、抬腕唤醒和原有拨轮演示控制正常；不把编译通过当作实机验收。
- 若报告恢复失败，停止采集并重启手表，先排查传感器问题。
- 数据确认后再做阶段 9B：检测抬手后的准备姿态与停稳 → 记录横向动作 → 判断方向和有效性 → 单次触发 → 锁定至手臂放下。先离线回放这些数据验证，再接入阶段 8 的翻页发送接口；终点刹停和放手不得触发反向翻页。
- 上位机改动留到识别方式确认后；第 7 阶段继续跳过。
