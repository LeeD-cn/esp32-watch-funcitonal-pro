# 阶段 3：新增游戏设计

更新日期：2026-09-09  
状态：贪吃蛇已实现并编译通过，待实物验收；本阶段严格只新增一个游戏。

## 1. 设计边界

- 保留飞机大战及其已有分数记录。
- 本阶段及当前总计划只新增一个游戏，不继续安排第二个新增游戏。如以后产生新需求，再单独评估。
- 适配 240×240 屏幕及拨轮三种输入：上拨（KEY1）、按下（KEY2）、下拨（KEY3）。
- KEY1/KEY3 在按住约 450 ms 后会每 120 ms 重复触发；游戏必须过滤不合适的连续输入。
- KEY2 当前在按下时产生事件，释放时另有事件，没有独立长按事件。本阶段优先选择不依赖 KEY2 长按的玩法，避免改变所有现有页面的确认键行为。
- 使用 LVGL 基础图形和现有字体，不加入游戏位图资源。阶段 2 构建产物在 3 MiB 应用分区内还剩 508,976 字节（16%），实现后重新测量。
- 当前 `sdkconfig` 未启用 PSRAM，因此即使芯片带 8 MB PSRAM，也不能把它计入当前运行内存预算。
- 新游戏有独立页面、状态和清理接口；从游戏中心进入，退出后返回并保持选中该游戏。

## 2. 选型记录

| 方案 | 拨轮操作 | 优点 | 主要限制 | 估计工作量 |
|---|---|---|---|---|
| 贪吃蛇（推荐） | 上拨左转、下拨右转、按下暂停 | 规则熟悉，三键分工自然；只需基础图形；可做完整开始/暂停/结束/重玩流程 | 转向方式需要在首次进入时说明；需过滤重复转向 | 中等 |
| 方向反应 | 根据屏幕的上/下提示拨动，按下暂停 | 实现短、占用小，易测试 | 玩法较单一，与“新增游戏”的展示效果一般 | 较低 |
| 三键序列记忆 | 观察上/按/下序列并复现 | 能完整使用三种输入，玩法与飞机大战差异大 | KEY2 同时作为答案后缺少自然的中途退出键；若增加长按会影响全局按键设计 | 中等偏高 |

用户最终选择“贪吃蛇”并保存历史最高分；其余候选不进入当前总计划。

## 3. 贪吃蛇玩法提案

### 3.1 规则

- 棋盘采用离散网格。建议 12×12，每格 14 px，游戏区约 168×168；最终尺寸在真机显示时微调。
- 蛇以固定节拍自动前进，吃到食物后长度加一、分数加一，并生成新食物。
- 撞到边界或自身后结束。第一版不做穿墙、障碍物、关卡或道具。
- 每得到若干分略微加速，并设置最小移动周期，保证拨轮仍可操作。
- 随机食物只能出现在空格；蛇占满棋盘时按胜利结束，避免随机生成死循环。

### 3.2 输入

| 状态 | 上拨 KEY1 | 下拨 KEY3 | 按下 KEY2 |
|---|---|---|---|
| 说明/待开始 | 切换底部选项 | 切换底部选项 | 确认“开始”或“返回” |
| 游戏中 | 相对当前方向左转 90° | 相对当前方向右转 90° | 进入暂停菜单 |
| 暂停 | 选择“继续/结束” | 选择“继续/结束” | 确认，默认选中“继续” |
| 游戏结束 | 选择“再来一次/返回” | 选择“再来一次/返回” | 确认，默认选中“再来一次” |

转向规则：一次移动节拍最多接受一次有效转向；忽略同一节拍内因按键重复产生的额外事件。采用“左转/右转”而非绝对上/下，因此两个拨动方向在蛇朝任意方向时都有稳定含义，也不会直接反向撞到自身。

### 3.3 页面布局

```text
┌────────────────────────┐
│ 贪吃蛇        分数  08 │  约 28 px
│ ┌────────────────────┐ │
│ │                    │ │
│ │      网格游戏区     │ │  约 168×168
│ │   蛇头/蛇身/食物    │ │
│ │                    │ │
│ └────────────────────┘ │
│  上拨左转  下拨右转    │  约 28 px
└────────────────────────┘
```

- 蛇头、蛇身、食物使用不同颜色和形状；颜色兼顾黑色背景下的对比度。
- 暂停和结束使用覆盖层，不创建另一套完整游戏对象。
- 首次说明页显示控制方法，开始后底部只保留短提示，避免遮挡棋盘。
- 中文字符尽量复用现有字库；确有缺字时生成仅包含所需字符的小字库。

## 4. 软件结构与内存方案

新增 `watch_snake.c/.h`，内部管理：

- 单个棋盘 LVGL 对象；蛇身不为每个格子或蛇段创建 LVGL 对象；
- 蛇坐标数组、长度、当前方向、待应用方向、食物位置和分数；
- `READY / RUNNING / PAUSED / GAME_OVER / WIN` 状态；
- 一个 LVGL timer，按移动周期更新逻辑；
- 返回游戏中心请求及可选的新纪录状态。

建议接口与现有游戏模式保持一致：

```c
lv_obj_t *watch_snake_create(lv_obj_t *parent);
void watch_snake_reset(void);
void watch_snake_start(void);
void watch_snake_on_key(watch_key_t key);
bool watch_snake_wants_back(void);
void watch_snake_destroy(void);
```

`watch_game.c` 增加游戏菜单项与 `GAME_MODE_SNAKE`，负责创建、显示、隐藏、销毁和返回。菜单从单一“飞机大战”卡片调整为两项纵向列表，焦点顺序为“返回 → 飞机大战 → 贪吃蛇”，上/下拨循环选择。

状态和显示仍在现有 LVGL 主任务内运行，不另建 FreeRTOS 任务。棋盘通过一个对象的绘制事件画蛇身和食物，不使用约 55 KiB 的 168×168 RGB565 Canvas，也不建立 144 个格子对象。蛇身采用固定长度数组或环形队列；12×12 棋盘可将格子位置编码为 0～143 的 `uint8_t`，完整蛇身坐标只需约 144 字节。游戏过程中不随蛇增长申请内存；退出时删除 timer 和页面对象，清空上下文。

### 4.1 容量与运行内存预算

| 项目 | 设计目标 | 检查方式 |
|---|---|---|
| 固件增量 | 目标不超过 60 KiB；超过 96 KiB 必须先分析字体、资源和符号来源 | 比较阶段 2 的 2,636,752 字节与新 `.bin` |
| 应用分区余量 | 完成后尽量保留至少 400 KiB，为后续通信功能预留空间 | 构建末尾 `check_sizes.py` 输出 |
| 页面运行堆增量 | 目标不超过 8 KiB，实测值为准 | 进入游戏前、运行中、退出后的 8-bit heap 与最大连续块 |
| 退出后残留 | 连续进入/退出后，空闲堆及最大连续块不持续下降；允许日志和系统活动带来的小幅波动 | 20 次循环并比较首尾数据 |
| 任务/栈 | 不新增 FreeRTOS 任务和任务栈 | 代码检查及运行日志 |
| 动态分配 | 不随蛇长度增长分配；不使用全屏/棋盘 RGB565 Canvas | 代码检查 |

实现时可增加只在调试配置启用的内存日志，使用 ESP-IDF heap 查询接口记录页面进入前、创建后、运行中及销毁后的空闲内存和最大连续块。验收关注趋势，不能只看一次 `free heap` 数字。

主要修改文件：

- `main/watch_game.c`
- `main/watch_snake.c`、`main/watch_snake.h`
- `main/CMakeLists.txt`
- 按需新增小字库并加入构建
- `DEVELOPMENT_PLAN.md`

## 5. 分数保存选择

建议保存一个“历史最高分”，使用独立 NVS 命名空间，例如 `snake_score`。仅在游戏结束且刷新纪录时写一次 Flash，不在每次吃到食物时写入。

这个选择会增加少量代码和一次持久化验收，但能让新增游戏更完整。若用户希望本阶段尽量小，可暂不保存，只显示本局分数。

## 6. 实施步骤

1. 调整游戏中心为两个菜单项，保留飞机大战原行为。
2. 建立贪吃蛇页面和状态机，先实现固定速度移动、转向、食物和碰撞。
3. 加入待开始、暂停、结束和重玩交互；处理重复按键与 timer 生命周期。
4. 加入最高分持久化，只在刷新纪录时写入 NVS。
5. 构建并记录固件大小、分区余量和警告，超过预算时先定位原因。
6. 在调试固件中记录进入、运行、退出及循环操作的堆内存变化。
7. 用户烧录后按验收清单实测；发现输入方向、布局或内存趋势问题时做小范围调整。

## 7. 验收清单

- 游戏中心可循环选择返回、飞机大战和新游戏，选择框位置正确。
- 飞机大战可以进入、移动、退出、结算和查看分数，行为未退化。
- 贪吃蛇可开始；上拨始终左转、下拨始终右转；长时间按住不会在单个移动节拍内连续多次转向。
- 食物不生成在蛇身上；吃到后只增长一次并正确计分。
- 撞墙、自撞均进入结束状态；再来一次清空上局状态。
- 游戏中按下可暂停；暂停不移动；继续后节拍正常；选择结束后返回游戏中心。
- 至少连续进入、退出和重玩 20 次，页面、timer 和对象无明显残留或崩溃，空闲堆与最大连续块不持续下降。
- 若启用最高分：刷新纪录后重启手表仍保留；未刷新纪录不产生多余写入。
- 应用固件未超过 3 MiB 分区，记录新增前后大小。

## 8. 实现与构建结果

- 新增 `main/watch_snake.c`、`main/watch_snake.h` 和最小中文字符集 `main/fonts/snake_font_20.c`。
- 修改 `main/watch_game.c` 接入菜单和页面生命周期，修改 `main/CMakeLists.txt` 纳入构建。
- 菜单字号修正后的固件从阶段 2 的 2,636,752 字节增加到 2,647,056 字节，增量 10,304 字节；应用分区剩余 498,672 字节，满足预算。
- 游戏中心的“贪吃蛇”使用单独的 26px 三字小字库，与“飞机大战”字号一致；游戏内部仍使用 20px 字体。
- ELF 符号显示静态 `s_snake` 上下文为 292 字节；未启用 PSRAM，也未新增任务或 Canvas。
- 运行时在页面创建前后、每局开始、销毁前后输出 8-bit 空闲堆和最大连续块，供真机验收页面堆增量及循环进出趋势。
- 当前只完成代码检查和 ESP-IDF 5.2.7 构建；实物操作、显示效果和运行堆趋势尚未验收。
