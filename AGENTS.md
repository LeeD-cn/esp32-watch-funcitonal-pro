# 手表项目工作约定

适用范围：本目录及子目录。用户当前明确指令优先于本文；不要把本文件解读为额外的审批流程。

## 开始任务

- 使用中文交流，用户熟悉 VS Code / Arduino IDE，但正在学习 ESP-IDF。解释关键改动与验证方法，避免只罗列术语。
- 先读 `DEVELOPMENT_PLAN.md` 的进度表、当前阶段、待定事项和最近交接记录，再检查实际文件。对话分支可能继承旧记录，磁盘状态必须重新核对。
- 明确本次指定阶段及完成目标。先只读定位相关代码，再简述实现路径；用户已要求实施时继续完成，不为普通实现细节重复征求许可。
- 检查 Git 状态（若存在）和用户未提交改动。保留现有改动，不以旧对话内容覆盖当前文件。
- 不预先阅读整套 LVGL / ESP-IDF 库；先定位本阶段相关模块，按需扩展。

## 阶段内的调整权限

- 用户允许根据源码、编译结果和实测情况合理调整每阶段的具体实现。总计划是目标和验收约定，不是逐行实现规格。
- 可直接决定：函数/文件拆分、变量命名、NVS 键与内部数据结构、输入校验、错误处理、字体与布局微调、与本阶段相关的小范围重构，以及必要的公共接口补充。
- 可拆分本阶段为更小步骤，或提前实现后续阶段也会复用的最小基础能力；说明原因和影响，不借机提前做完整后续业务。
- 尊重已明确的产品要求，如最多 3 个预设、电脑为协同状态最终来源、结束默认取消、翻页单次触发及断线不补发。不能为了方便实现而删除或弱化验收要求。
- 若需要改变用户可见的操作方式、功能取舍、支持平台、持久数据含义、跨阶段接口契约或电源行为，先简述原因、方案和影响，请用户决定。已在会话明确授权的选择直接沿用。
- 分区迁移、擦除数据、框架大版本升级和整体重构不属于“小调整”；先完成只读评估和可审阅方案。不要以“可能更好”为由扩展范围。
- 记录实际调整及原因；把总计划中被替代的方案更新为最终方案，不保留相互矛盾的执行要求。

## 环境和构建

- 现有基线：ESP-IDF 5.2.7、手表端 C、FreeRTOS、本地 LVGL 9.6.0-dev、240×240 屏幕。路径和版本使用前核对。
- 框架：`D:/Espressif/frameworks/esp-idf-v5.2.7/`；工具根：`D:/Espressif`；Python：`D:/Espressif/python_env/idf5.2_py3.11_env/Scripts/python.exe`。
- 使用当前工作目录下的 `build/`。其他工作树必须有自己的构建目录，不能复用原目录的绝对路径缓存。
- 普通 PowerShell 使用 ESP-IDF 独立 Python 激活环境，避免系统 Python 3.12 混入。已激活的 ESP-IDF 终端可直接运行 `idf.py build`。参考：

```powershell
$env:IDF_TOOLS_PATH = 'D:\Espressif'
$env:PATH = 'D:\Espressif\python_env\idf5.2_py3.11_env\Scripts;' + $env:PATH
. D:\Espressif\frameworks\esp-idf-v5.2.7\export.ps1
idf.py build
```

- 新增源文件同步 `main/CMakeLists.txt`；保留现有 `sdkconfig` 和分区配置，不随意 `set-target`、清空配置或升级依赖。
- 有代码改动时执行相关构建；检查失败、警告与固件大小。构建成功后不无故重复全量构建。
- 大型工具下载/安装优先给用户图形操作指导；命令行检查和正常构建可直接进行。

## 代码与硬件边界

- 沿用现有页面生命周期及 `watch_ui.c` 调度；网络/采样回调通过队列等方式通知 UI，不在任意线程直接操作 LVGL。
- 退出页面释放对象、定时器和页面专属任务；处理迟到回调、按键残留和重入。
- NVS 仅在保存/编辑/删除等必要时写入；区分默认值、无效输入和存储失败；不按秒持久化计时。
- 共享传感器和总线接口需兼容 RTC、电量、抬腕唤醒。新增陀螺仪或采样模式要明确进入/退出后的恢复。
- 当前息屏会关闭 Wi-Fi；按总计划在通信阶段解决，不能默认联网与睡眠已经兼容。
- 保留独立电源键的约定行为；改变行为须有用户决定。
- 每阶段关注应用分区余量，不能把整片 16 MB Flash 等同于应用可用空间。不要修改分区来绕过容量错误而不处理数据迁移。
- 对关键状态、不明显的单位/坐标映射、去重和持久化约束写简短注释；不为每行代码补冗余注释。

## 验证与交接

- 分开记录：代码已实现、构建已通过、实物待验证、用户已验收。没有实物证据不能标记“已验收”。
- 简单 UI 改动以构建和针对性实测清单为主；计时状态机、协议去重和算法等纯逻辑采用有意义的测试。
- 给用户本阶段具体实测步骤和预期结果；覆盖受影响的旧功能，不要求每个微小改动重测所有功能。
- 默认用户在 VS Code 执行烧录。仅在用户授权本次或持续授权范围内操作设备；重核实际端口、固件和备份，不每阶段重做完整原厂备份。
- 不覆盖或删除 `backups/` 原厂备份，不把设备配置或备份上传到远程；恢复写入未实测，不能承诺必然可恢复。
- 每次阶段交接或实现路径有实质变化，更新 `DEVELOPMENT_PLAN.md`：状态、涉及文件、决策/调整原因、构建证据、实测结果来源、已知问题和下一步。
- 有 Git 时记录对应提交号/分支；无提交时明确“未提交”，不捏造源码版本。当前产物每次构建会覆盖，不能把历史构建结果当作新改动的验证。
- 最终答复简述改了什么、为何这样改、验证了什么、尚需用户实测什么。

## 对话分支与代码版本

- 对话分支与 Git 分支分别管理讨论和代码历史；不要假定建立对话分支就复制/隔离了源码。
- 默认各阶段按依赖顺序在同一工作目录推进，避免多个对话同时写同一目录或占用同一串口。
- 下一阶段使用包含前阶段已完成改动的当前源码；使用独立 Git 分支时，从已整合的最新基线建立，不能所有阶段都从初始版本分出。
- 需要并行时再明确独立 worktree、公共接口、独立构建目录和集成顺序；未经用户要求不启用并行子代理。
