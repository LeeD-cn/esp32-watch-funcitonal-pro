# 后台计步首轮测试

当前版本验证 BMI270 硬件计步、后台累计、31 天历史和计步器页面。

## 1. 烧录并检查启动

在 VS Code 中选择手表串口，执行 `ESP-IDF: Build, Flash and Start a Monitor`。
启动日志应出现：

```text
watch_steps: hardware step counter started, today=... raw=...
```

若出现 `BMI270 step counter unavailable`，保存该行及其前后日志，先不要继续测试。

## 2. 查询当前步数

先进入主菜单的“计步器”，检查今日页数字能够显示；进入“历史步数”后，首次升级应依次看到今天以及 2026-09-14～09-08 的记录，旧日期初始为 0。检查：

- 历史行能够逐行选中，白色矩形框跟随当前行；按下普通记录不产生动作。
- 最上一行向上回到左上角返回键；按下返回今日页。
- 记录多于 8 条后，在可见底行继续向下会逐行滚动；最旧记录继续向下回到最新记录。

也可以通过 USB 查询数值。先关闭 VS Code 串口监视器，并断开上位机的 USB 串口，然后在项目根目录运行：

```powershell
python tools/query_steps.py --port COM3
```

把 `COM3` 换成手表的实际串口。若提示缺少 pyserial：

```powershell
python -m pip install pyserial
```

## 3. 走路测试

1. 记下首次查询的“今日步数”。
2. 将手表正常戴在左手，自然连续走 100 步；不要用手原地摇晃代替走路。
3. 停下后等待 3 秒，再运行一次查询。
4. 记录实际步数、程序增量和误差。首轮目标是确认持续增长且不会明显翻倍，不以一次 100 步测试校准最终精度。

## 4. 后台与重启测试

1. 主动熄屏，正常走 50 步，再唤醒并查询，确认睡眠期间仍有累计。
2. 记录步数，重启手表后再次查询，确认今日总数不会归零或重复增加。
3. 若日期已经由 RTC/联网同步，跨自然日后的今日值应从 0 开始，前一天最终数值应进入历史记录。

请把页面照片、100 步结果、熄屏 50 步结果，以及任何 `watch_steps` 错误日志发回来，再决定是否做灵敏度补偿。
