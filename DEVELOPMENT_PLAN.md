# ESP32-S3 手表软件分阶段开发计划

更新日期：2026-09-15
计划来源：用户实测后的功能需求，以及当日对当前源码的只读检查。  
当前状态：阶段 0～6、8、9 已完成并通过实机验收；阶段 7 已实现并编译通过、待实机验收；阶段 10 尚未开始。

## 1. 项目目标与使用方式

复用成品硬件与现有可用驱动，调整本地应用，新增 Windows 电脑协同专注及演示遥控。采用小步开发，每阶段形成一个可演示、可验收的结果。

后续接手本项目时，先阅读本文的“当前进度”“关键约束”“待定事项”，再检查实际代码及工作区变化。本文记录计划，不能代替实际编译和实物验证。

- 每次只实施当前指定阶段；本计划不是一次性实施全部功能的授权。
- 每阶段开始前明确界面、按键行为、数据结构和影响文件。用户已允许阶段内合理调整：普通实现细节由模型决定并记录；影响用户体验、功能取舍、数据兼容或电源行为的待定选择才需与用户确定。具体边界见 `AGENTS.md`。
- 编译通过与实物验收分开记录。仅编译通过，阶段仍为“待实测”。
- 完成一阶段后更新本文：修改文件、验证结果、遗留问题、下一步；不要只留在聊天中。
- 保存可工作的源码版本和对应固件；建立 Git 基线时先检查现有仓库，不覆盖用户改动。
- 安装大型工具优先由用户使用图形安装器；日常编辑、构建、烧录继续使用 VS Code。

### 分阶段对话的接续方式

- 用户计划在分支对话中使用 GPT-5.6 Sol 逐阶段实现；模型选择由用户界面控制，本文不强制模型或推理档位。
- 对话分支各自聚焦一个阶段；若指向同一工作目录，则串行修改，上一阶段交接后再开始下一阶段。
- 每次启动阶段先读取本计划及 `AGENTS.md`，核对当前磁盘代码，不依赖分支创建时继承的旧聊天状态。
- 截至 2026-09-09 基线准备时，当前目录已初始化 Git 仓库，分支为 `master`；本次建立首个源码基线提交。每阶段完成后保存一个可识别版本。对话历史与原厂固件备份都不能替代源码版本记录。
- 若以后使用 Git 分支/worktree，后续阶段应以含前置阶段改动的已整合基线开始；多个工作树各自构建，设备串口串行使用。
- 阶段交接记录补充：实现调整及原因、所依据的源码版本、是否已提交。计划条目可以细化或拆成子阶段，核心需求及验收不能未经决定削减。

## 2. 已验证基线与当前源码事实

| 项目 | 已知情况及证据边界 |
|---|---|
| 工程 | 当前根目录是手表端 ESP-IDF C 工程；根 README 仍是 Hello World 模板 |
| 环境 | ESP-IDF 5.2.7；安装位置 `D:/Espressif/frameworks/esp-idf-v5.2.7/`；Python 环境 `D:/Espressif/python_env/idf5.2_py3.11_env/` |
| GUI | 本地 `components/lvgl/lv_version.h` 标注 9.6.0-dev；240×240 屏幕，实体拨轮和独立电源键 |
| 硬件 | 此前 COM18 实测 ESP32-S3 revision v0.2、16 MB Flash、8 MB PSRAM；端口使用前重新核对 |
| 构建与实测 | 原功能工程已成功编译并由用户烧录，用户反馈“全部正常”；不代表未来新增功能已验证 |
| 原厂备份 | `backups/watch-original-20260907-16MB.bin`，当日检查文件存在且为 16,777,216 字节；同名 JSON 有 SHA-256；恢复写入未实测 |
| Flash 布局 | `partitions.csv`：NVS、factory 应用、img_store、storage；factory 从 0x50000 开始，容量 0x300000（3 MiB） |
| 容量余量 | 阶段 5 左右补偿修正版构建产物为 2,654,448 字节，3 MiB 应用分区剩余 491,280 字节（16%） |
| 指南针 | 已接入 BMI270 重力向量、三轴磁校准和水平基底投影；当前成品已完成轴向确认、校准持久化和前后倾斜实测，用户接受阶段 5 当前结果 |
| 既有校准 | 当前成品已成功保存三轴磁校准且日志保持 `cal=1`；固定地磁偏角和安装偏移仍属于现场精度参数 |
| BMI270 | 保持 25 Hz、±4g 和仅开加速度计；新增带单调时间戳的公共三轴 mg 采样接口，未开启陀螺仪 |
| 按键 | KEY2 有按下/释放事件，没有独立 KEY2 长按事件；新增长按需处理短按误触发和旧页面兼容 |
| 电源 | `main.c` 当前息屏会调用 `watch_wifi_stop()` 并进入轻睡眠；这与持续无线控制存在冲突 |
| 上位机 | 当前工作区不含电脑端源码工程；上层 `03_Software/ESP-Watch-UpperComputerv1.1.zip` 存在，尚未检查内容与可构建性 |

## 3. 总体路线和当前进度

本地功能先完成，公共通信先于协同应用，实体按键遥控先于手势识别。游戏与本地番茄钟相互独立，必要时可调换顺序。

| 阶段 | 独立交付内容 | 依赖 | 状态 |
|---|---|---|---|
| 0 | 开发环境、原始编译、原厂备份、实物烧录验证 | 无 | 已完成 |
| 1 | 电子名片改为设备信息，持久保存拥有者/设备名/ID | 0 | 已验收 |
| 2 | 番茄钟最多 3 个本地倒计时预设 | 1 的存储约定 | 已验收 |
| 3 | 游戏中心新增一个贪吃蛇游戏 | 0 | 已完成（用户于 2026-09-09 确认） |
| 4 | 指南针 UI 与指北箭头 | 0 | 已完成（用户于 2026-09-09 确认） |
| 5 | 共享传感器接口、调平提示、倾斜补偿 | 4 | 已完成（用户于 2026-09-09 确认） |
| 6 | Windows 上位机基础窗口与统一无线连接 | 1；确定电源策略 | 已验收（`stage-6-wireless-host`） |
| 7 | 电脑协同专注模式与本地历史 | 2、6 | 已实现、编译通过待实测 |
| 8 | 演示遥控应用，拨轮控制 WPS 演示（兼容 PowerPoint） | 6；补充 KEY2 长按处理 | 已验收（用户于 2026-09-13 确认） |
| 9 | 左右挥手识别与防误触 | 5、8 | 已验收（用户于 2026-09-13 确认） |
| 10 | 整体回归、指标评测与展示交付 | 1～9 | 未开始 |

## 4. 各阶段范围与验收

### 阶段 1：设备信息替换电子名片

目标：主菜单原“电子名片”入口改为“设备信息”，显示拥有者、设备名和 ID 编号；数据保存到手表 Flash 中的 NVS，掉电后保留。

工作内容：
- 定义字段、长度限制、默认值、配置版本及读写失败处理；独立命名空间/键，避免干扰 Wi-Fi、天气和图片配置。
- 确定 ID 含义：用户管理编号或设备自动生成的固定标识，不混为一谈。拥有者与设备名的编辑方式在开工时确定。
- 建议第一版用已有 USB JSON 配置入口写入文字字段，手表负责显示；提供可复现的配置示例，后续阶段 6 再接电脑端表单。
- 替换页面及菜单名称，检查中英文字体、长文本换行/截断和返回操作。
- B 站统计、二维码相关调用按依赖关系处理，避免删除仍被其他功能使用的图片或配置接口。

主要入口：`watch_e_card.c/.h`、`watch_menu.c`、`watch_ui.c`、`watch_config.c/.h`、`watch_serial_config.c`、`watch_language.c`；重命名模块时同步 `main/CMakeLists.txt`。

验收：三个字段正确显示；修改后重启/断电仍保留；空值和超长输入处理明确；原 Wi-Fi、天气配置及表盘图片正常；不再展示旧电子名片业务。

### 阶段 2：保存的时钟

目标：在番茄钟“时间设置”右侧新增“保存的时钟”，最多保存 3 个倒计时预设，可新增、修改、删除和一键启动。

工作内容：
- 使用最多 3 个稳定槽位，保存有效标记及倒计时总秒数；第一版是否命名预设在开工时确定，默认可直接显示时长。
- 定义空列表、空槽位、已满列表、编辑确认/取消、删除确认；零时长不可启动，上限与现有时间设置统一。
- 选择预设并启动后自动回到“番茄钟主页面”，应用预设且立即开始倒计时；这里不指手表主表盘。
- 原有手动设置和独立计时模式继续可用；已有运行中计时时不得静默覆盖，先明确替换行为并确认。
- 预设写入 NVS；仅在用户保存/修改/删除时写入，不按秒写 Flash。

主要入口：`watch_tomato_clock.c/.h`，新增预设存储模块，按需接入 `watch_config`。

验收：0、1、3 个预设均可操作；第 4 个不能新增；修改、删除、取消正确；掉电保留；一键启动后页面和倒计时正确；正计时/原有手动功能回归通过。

### 阶段 3：新增游戏

目标：在保留飞机大战的游戏中心内，只增加一个可独立进入和退出的游戏；当前总计划不安排第二个新增游戏。

工作内容：先选择适合“上拨/按下/下拨”的一个游戏并确认规则，再实现菜单、游戏页面、状态与退出，不扩展多个玩法。实现必须同时控制 Flash 固件容量和运行 RAM；当前 PSRAM 未启用，详细预算见 `STAGE3_GAME_DESIGN.md`。

主要入口：`watch_game.c/.h`、新游戏模块、`main/CMakeLists.txt`、字体/图片资源。

验收：可以开始、操作、结束和重新开始；按键映射明确；反复进出页面无资源持续增长；飞机大战仍正常；固件容量检查通过。

最终选择：只新增“贪吃蛇”，并保存一个历史最高分。实现细节、按键规则和内存预算见 `STAGE3_GAME_DESIGN.md`。

### 阶段 4：指南针界面与箭头

目标：改善视觉层次，加入明确的指北箭头、航向数字及方位标识。

工作内容：先确定 240×240 布局和箭头语义；建议固定“表盘上方为朝向参考”，箭头表示北方相对屏幕的位置。延续现有旋转圆盘时避免重复旋转导致方向错误；处理 359°/0° 跳变和读数异常显示，为下一阶段调平提示留空间。

主要入口：`watch_compass.c/.h`。

验收：水平放置旋转一周，箭头、刻度和数字相互一致；跨 0° 显示稳定；文字不重叠；磁力计不可用时有明确状态。

### 阶段 5：传感器接口、调平与倾斜补偿

目标：提供可复用的传感器采样接口，实现调平显示，并在适用的静止/缓慢运动条件下改善倾斜时航向。

工作内容分三个内部检查点：
1. 暴露带时间戳、单位、有效状态的三轴加速度采样；统一传感器与表盘坐标系，确认正负方向和安装映射。设计模式切换与总线访问，复用现有 I²C，保留抬腕唤醒。
2. 根据重力方向给出水平气泡或倾斜指示；静止时显示“已调平”，运动中提示读数暂不稳定。加速度计只能提供调平指导，不会自动机械调平手表。
3. 在统一坐标系内使用磁场 XYZ 与重力方向做倾斜补偿；区分磁力计偏置/比例校准、传感器轴映射、地磁偏角和倾斜补偿。固定 -3.2° 偏角与 15° 安装偏移需重新核实，不当成通用正确值。

原驱动有校准接口，但实测校准流程及参数保存仍需检查；为本机建立必要校准流程。强运动、磁干扰或接近姿态奇异区域时给出无效/不稳定状态，不能把补偿表述成消除所有误差。

主要入口：`watch_bmi270.c/.h`、`qmc5883p.c/.h`、`qmc5883p_port.c`、`watch_compass.c`，按需增加校准存储。

验收：实测三轴方向；同一朝向下水平、前后/左右倾斜（例如 ±15°、±30°）比较补偿前后误差；记录测试环境与参考方法；调平指示方向正确；运动异常可识别；退出后抬腕唤醒正常。精度目标在采样后确定，不能仅凭公式通过验收。

### 阶段 6：上位机窗口和统一无线连接

目标：建立一套供专注与演示共享的双向连接，先让连接、断线、重连和确认可靠工作。

电脑窗口第一版包含：
- 设备连接：在线/离线、连接/断开、地址与必要配置；补充设备信息编辑。
- 专注任务：项目名、计划时长、开始/暂停/结束及简单历史区域，本阶段先建立入口。
- 演示遥控：启用控制开关、最近指令、手势灵敏度入口，业务分别在阶段 8、9 落地。

技术方向：Windows + Qt/C++；先检查上位机压缩包、Qt 版本和构建方式，再决定复用原工程还是新建精简程序。电脑端文件建议纳入明确的独立目录并与固件构建隔离，实际目录在本阶段确定。

连接方向：手表连接 Windows 热点，电脑监听该网络接口，手表主动连接配置的电脑地址与端口；首次借用 USB JSON 配置写入地址，日常无线连接。手动地址必须可查看、修改；自动发现放后续。建议采用 WebSocket + JSON，最终协议在本阶段固化，并核实 ESP-IDF 5.2.7 对应组件兼容性。

通信约定：
- 统一处理握手、设备识别、心跳、断线检测、重连、错误提示、请求确认和协议版本。
- 请求携带连接会话标识、唯一操作 ID、操作类型及有效期；状态携带任务 ID 和递增版本。电脑对重复 ID 返回已有结果，过期/旧会话请求不执行。
- 翻页指令不离线排队、不在重连后补发；确认超时不能擅自换新 ID 重发操作。
- 确认区分“收到”“已处理/拒绝”，UI 不把收到消息当成已完成操作。
- 首版限定一只手表与一台电脑的一条控制会话，明确配对/访问校验；无关客户端不能触发电脑控制。
- 网络回调通过队列向 UI 任务投递，遵守现有 LVGL 单任务操作约定；不能直接在网络线程修改页面对象。
- 做好中文 JSON、长度边界、未知命令和协议版本不匹配处理。

必须在本阶段解决电源冲突：当前息屏会断 Wi-Fi。建议协同专注时允许按原逻辑休眠/断线，电脑继续计时，唤醒后重新同步；演示控制启用且亮屏时抑制自动息屏，KEY4 主动息屏/关机仍有效并停止控制。若要求熄屏仍可无线操作，则需另行实现保持连接的显示关闭模式并测功耗。最终选择记入“待定事项”，不可悄悄更改电源行为。

验收：手动地址可连接；热点不可用、地址错误、电脑关闭、反复断线重连都有明确反馈；重复操作只处理一次；旧会话指令丢弃；退出页面不导致悬空回调；本地功能离线仍可用；息屏/唤醒策略完成实测。

### 阶段 7：电脑协同专注

目标：番茄钟新增“连接电脑”，保留本地独立模式；电脑是任务和计时状态的最终来源。

工作内容：
- 电脑输入项目名称和计划时长（例如“复变函数习题”，25 分钟），下发当前任务；第一版同时只运行一个协同任务。
- 状态至少包含待开始、专注中、已暂停、已完成，以及确认中途结束后的“已中止”；电脑串行处理来自两端的操作。
- 两端均可开始、暂停、继续；手表只发送请求，电脑处理后同步快照。使用电脑单调时间计算有效专注时长，暂停时间不计入，不能靠 UI 刷新次数计时。
- 手表可依据快照在同步间隔内平滑显示剩余时间；本地到零只等待电脑确认，不能自行宣布完成；用版本号防止旧快照覆盖新状态。
- 手表顶部显示连接状态，中部显示项目名，长名称换行/截断；大数字显示剩余时间，辅助显示计划时长和任务状态。
- 上/下拨切换底部主操作与“结束”；按下执行当前选中项，主操作随状态为开始/暂停/继续。
- 选择结束并按下显示“结束本次专注？”确认，默认取消；电脑端中途结束同样确认。KEY4 保留独立电源行为。
- 连接失败显示重试/返回；断线后停止任务控制并明确标识状态已失联，电脑继续计时；重连完全以电脑快照恢复。
- 离开页面不结束任务；再次进入恢复当前任务。独立计时与协同任务的数据互不覆盖，切换时明确已有本地计时的处理。
- 电脑持久保存项目名称、计划时长、实际专注时长、完成/中止状态；最终记录按任务 ID 去重。第一版可用本地 JSON，无需服务器或云端。
- 关闭电脑程序时，运行中及暂停中的未结束任务都先确认；取消则保留程序，确认则结束并记录。历史保留，但第一版不做活动任务跨重启恢复；异常退出不伪造完成记录。

主要入口：番茄钟模式入口、新协同页面、公共连接模块、Qt 专注状态机与历史模块。

验收：两端操作一致；同时操作有确定结果；暂停不累计时长；正常完成和中途结束各记录一次；长中文名正常；断线/重连/离页返回/电脑关闭取消均符合约定。

### 阶段 8：拨轮演示遥控

目标：主菜单新增“演示遥控”，以 Windows + WPS 演示放映模式验证上一页/下一页；保留 Microsoft PowerPoint 兼容作为附加回归。

工作内容：
- 页面显示电脑连接状态、演示控制开启/暂停、手势开启/关闭、最近操作反馈。不读取真实页码时不显示推算页码。
- 上拨上一页、下拨下一页，每次拨动只触发一次，不做按住连续翻页。
- KEY2 短按切换手势识别开关；本阶段手势算法未完成时显示“暂未启用”，不能虚假显示可用。
- KEY2 长按退出并停止发指令；短按在释放后确认，长按不得先触发短按；离页时清理待处理事件。旧页面的 KEY2 行为需回归。
- 电脑提供“启用演示控制”开关，默认关闭；用户进入放映后开启，结束演示时关闭。电脑和手表页面两侧均满足条件才允许执行。
- 如果采用模拟按键，WPS 演示或 PowerPoint 放映窗口必须在前台；电脑验证前台进程及放映窗口，条件不符则拒绝，避免发给编辑窗口或其他应用。WPS 进程名/窗口特征以用户当前安装版本的实测结果为准。
- 反馈使用“翻页指令已发送/电脑已处理”等与证据一致的文字；只有真实读取并验证幻灯片状态，才能说“已确认翻页”。
- 断线直接丢弃翻页；重连后不补发。电脑重启或新控制会话默认关闭演示控制。
- 第一版不做打开文件、选择幻灯片、备注读取等功能。

验收：WPS 放映中上下拨各一次只翻一页；禁用、退出、断线或前台条件不符时不翻页；长按退出不切换手势；重连不突然翻页；鼠标翻页后 UI 不产生虚假页码。若电脑已安装 PowerPoint，再执行兼容回归，但不作为阶段 8 完成的前置条件。

### 阶段 9：左右挥手与防误触

目标：在拨轮可靠可用的基础上增加手势翻页，并给出实测指标。

工作内容：
- 扩展阶段 5 的 BMI270 接口，实际启用并读取陀螺仪，验证采样频率、时间戳、单位及加速度/角速度轴映射；退出识别后恢复低功耗模式。
- 动作约定：戴表手抬至胸前、保持约定姿态、停稳后快速向左/右挥动一次并复位。“胸前”只是使用约定，设备不能直接测出手相对胸口的位置。
- 先采集带标签的左右挥手、复位和普通动作数据，再确定阈值。佩戴左右手、表盘朝向和个人差异纳入测试。
- 状态机：准备姿态及停稳 → 动作开始 → 收集运动 → 方向及有效性判断 → 单次触发 → 冷却并等待重新停稳。
- 右挥下一页，左挥上一页；综合加速度变化、角速度与姿态，不用单轴正负号直接判定。
- 试验起点：准备停稳 0.3～0.5 秒；动作 0.2～0.8 秒；冷却 0.8～1.2 秒。均是待实测参数，不是已验证阈值。
- 检查主运动方向、其他方向干扰及动作时长；一个动作至多触发一次；冷却结束仍须重新停稳，收手不得触发反向翻页。
- 仅在演示页面、电脑允许控制、连接正常、用户主动开启手势时识别；短按可关闭手势但保留拨轮。断线或退出重置识别状态，重连后建议手动重新开启。
- 电脑灵敏度设置映射到经过验证的参数档位；不能任意调低安全边界造成持续误触发。

验收：分别记录左右成功率、漏识别、错误方向、收手误触发、自然动作误触发及响应时间；自然讲话、走动、普通抬腕、连续挥手均测试。每人每方向建议至少 20 次有效动作并单独记录自然活动时长；训练调参数据与最后评测数据分开。阈值与最终指标经实测确定。

### 阶段 10：整体验收与演示交付

目标：形成可复现展示的手表固件、电脑程序、配置说明和实测报告。

工作内容：
- 本地功能回归：设备信息、三个预设、原番茄钟、新旧游戏、指南针、菜单、设置、天气及仍保留的联网功能。
- 系统回归：开关机、自动息屏、KEY4、抬腕、充电/电量显示；页面反复进出、连接反复断开后资源无持续泄漏。
- 协同测试：重复/过期/乱序业务消息、网络抖动、断线、电脑关闭、前台切换、任务已结束后收到迟到请求。
- 每阶段记录固件容量；此阶段汇总运行内存、长时间稳定性、联网与传感器模式功耗。硬件有 8 MB PSRAM 不等于当前工程已启用，使用前核对 sdkconfig。
- 容量不足时先审查字体、图片和已弃用资源；分区迁移另列小步骤，明确图片/配置搬迁和恢复方式，不能因为整片 Flash 有空间就直接改分区。
- 保存源码版本、依赖版本、固件、电脑程序和使用说明；原厂个人备份不随公开源码或演示包分发。
- 展示重点：专注任务双向状态同步；拨轮和惯性手势双操作方式；提供识别率、误触次数/活动时间、响应时间等数据及测试条件。

验收：按书面操作步骤可重复完成演示；关键异常可恢复；构建及实测记录齐全；已知限制明确，不夸大精度或“已执行”的含义。

## 5. 待定事项与决策记录

这些事项不阻碍总计划建立，在相关阶段开始前确定。

| 事项 | 当前建议/需要确认 | 最晚确定阶段 |
|---|---|---|
| 设备 ID | 已决定：依据芯片 Wi-Fi Station MAC 生成并首次写入 NVS，固定且只读 | 已定（阶段 1） |
| 设备信息录入 | 已决定：第一版复用 USB JSON；拥有者/设备名最多 32 个 ASCII 字符，完整中文输入按容量和实际姓名字符后续扩展 | 已定（阶段 1） |
| 预设细节 | 已决定：第一版不命名，三个固定槽位直接显示时长；范围 00:00:01～99:59:59；运行或暂停中的计时禁止被预设替换并提示先清零 | 已定（阶段 2） |
| 新游戏 | 已决定并实现：只新增贪吃蛇，保存历史最高分 | 已定（阶段 3） |
| 指南针布局 | 已决定：圆盘继续旋转；屏幕顶部是固定朝向参考，中央红色箭头指向北方；航向数字放在下部。调平区域与精度目标在阶段 5 确定 | 已定（阶段 4）；阶段 5 续定 |
| 传感器轴映射 | 已由当前成品五姿态日志确认 BMI270：屏幕顶部 = 原始 Y 正、屏幕右侧 = 原始 X 正、屏幕朝外 = 原始 Z 正；QMC5883P 映射继续沿用原二维航向方向 | 已定（阶段 5） |
| 上位机工程 | 已决定：在 `upper_computer/` 建立 Qt 6.11.2 + CMake 工程，保留 USB 文字配置；原 v1.1 工具继续用于图片传输 | 已定（阶段 6） |
| 传输协议 | 已决定：TCP 长连接 + UTF-8 换行 JSON，默认端口 8765；配对令牌、协议版本、会话 ID、双向心跳；业务去重契约见 `WIRELESS_PROTOCOL.md` | 已定（阶段 6） |
| 息屏与连接 | 已决定：普通息屏沿用断 Wi-Fi，唤醒后自动重连；KEY4 保持原行为；演示启用时抑制自动息屏留到阶段 8 | 已定（阶段 6） |
| 演示软件与窗口 | 已决定：Windows + WPS 演示全屏放映为阶段 8 必测目标；兼容 PowerPoint。电脑验证前台进程和全屏窗口后模拟 Page Up/Page Down | 已定（阶段 8） |
| 任务切换 | 已决定：主菜单拆分为“计时器”（原本地正/倒计时）与紧随其后的“番茄钟”（电脑协同）；两者不再共享二级入口或状态 | 已定（阶段 7） |
| 电脑计时异常 | 已决定：首版采用 Qt 单调时钟，Windows 睡眠/锁屏期间继续累计；唤醒后刷新并同步，不做活动任务跨进程重启恢复 | 已定（阶段 7） |
| 手势适用范围 | 已确定左手佩戴、表盘朝外；动作流程为手臂放下 → 抬到胸前并短暂停稳 → 横向挥动 → 终点停止 → 直接放下，不横向返回；先独立 USB 采六轴，上位机暂不改；阈值与成功率目标待数据确认 | 9 |

## 6. 后续更新模板

每阶段结束后在此追加简短记录，同时更新进度表：

```text
日期 / 阶段：
状态：未开始 / 进行中 / 编译通过待实测 / 已验收 / 有阻塞
本次完成：
涉及文件：
源码基线/当前版本（无提交写“未提交”）：
实现调整及原因：
构建版本与固件大小：
实物测试结果（由谁验证）：
已知问题与待定选择：
下一步：
```

### 2026-09-09 / 计划建立

- 完成用户需求分阶段整理，并检查关键代码依赖。
- 阶段 0 沿用本会话已完成的构建、备份、烧录和用户实测记录；当日检查了安装配置、依赖版本、备份存在性及现有固件大小。
- 当前只新增本计划，没有修改功能源码，没有新构建或设备操作。
- 下一步：阶段 1，先确定设备 ID 与录入方式，再实现设备信息页面和持久保存。

### 2026-09-09 / 分阶段协作准备

- 新增根目录 `AGENTS.md`，规定读取计划、阶段内合理调整、构建验证、实测交接及分支协作方式。
- 用户允许按情况调整具体实现；普通内部实现自主推进，产品行为和数据/电源等实质变化仍需明确决策。
- 检查确认当前目录尚未建立 Git 仓库；本次未初始化仓库或提交代码。
- 功能阶段仍未开始；下一步建议保存源码基线，再在阶段 1 对话中确定设备 ID 含义与录入方式。

### 2026-09-09 / 本地源码基线

- 本次提交包含现有手表源码、LVGL、构建配置、总计划与工作约定；按 `.gitignore` 排除构建产物、原厂备份、旧 SDK 配置及机器专用 VS Code 配置。
- 功能阶段尚未开始，沿用此前构建与用户实测结果，本次没有新功能改动或重新烧录。
- 提交号通过 `git log` 查询；用户明确只需本地基线，不上传远程仓库。

### 2026-09-09 / 阶段 6 启动准备

- 用户确认阶段 1～5 已完成，本表据此补记阶段 3～5 的用户验收状态。
- 已从 `master` 最新阶段 5 提交创建本地分支 `stage-6-wireless-host`。
- 展示网络确定为 Windows 笔记本开启热点、手表连接热点、手表主动连接电脑端服务。
- 检查原上位机压缩包：它是 Qt Widgets + SerialPort 的 qmake 串口配置程序，没有无线通信模块；当前电脑装有 Qt 6.11.2 MinGW、Qt Network 和 Qt Creator，但缺少 Qt Serial Port 与 Qt WebSockets。
- 阶段 6 实现调整：采用 TCP 长连接 + 换行分隔 JSON，使用 Qt Network 和 ESP-IDF socket；不引入 WebSocket 组件，以控制固件容量。迁移并保留原上位机串口配置能力，因此开始电脑端构建前需通过 Qt Maintenance Tool 补装 Qt Serial Port。
- 默认首次通过 USB 写入电脑地址、端口和配对令牌；无线连接负责日常心跳、重连、状态与操作确认。详细协议在实现时形成独立文档。
- 电源默认沿用总计划：普通息屏允许断线，唤醒后重新连接并同步；KEY4 行为保持不变。演示控制期间的自动息屏抑制在阶段 8 接入。
- 本次尚未实现或构建阶段 6 功能；下一步是安装 Qt Serial Port，然后建立上位机目录和公共通信协议。

### 2026-09-09 / 阶段 6：上位机与统一无线连接

- 状态：已验收；分支 `stage-6-wireless-host`，阶段成果保存为本地 Git 提交，不上传远程。
- 环境：确认 Qt 6.11.2 的 Widgets、Network、Serial Port 均已安装；电脑端采用 CMake，构建产物与 ESP-IDF 的 `build/` 分离。
- 固件：配置结构和 USB JSON 增加 `host_addr`、`host_port`、`pair_token`，写入 NVS；新增后台 TCP 任务，处理 Wi-Fi 等待、5 秒连接超时、3 秒重连、配对握手、双向心跳和 15 秒断线判定。网络任务不直接操作 LVGL。
- 上位机：新增 `upper_computer/` Qt Widgets 程序，包含 USB 文字配置、IPv4 地址提示、TCP 监听、单手表认证、在线状态和日志；专注与演示页面仅保留入口，没有提前实现后续业务。原 v1.1 工具仍可用于 QR/封面图片传输。
- 协议：见 `WIRELESS_PROTOCOL.md`。采用 TCP 换行 JSON v1，默认端口 8765；电脑为服务端，手表主动连接；每次认证生成新会话，断线不补发未来的瞬时控制操作。
- 电源：普通息屏仍停止 Wi-Fi，KEY4 行为未改；唤醒后后台任务重新连接。演示控制期间是否抑制自动息屏在阶段 8 接入。
- 构建：ESP-IDF 5.2.7 `idf.py build` 通过，固件 `0x288b40` 字节，3 MiB 应用分区剩余 `0x774c0` 字节（16%）；Qt 6.11.2 MinGW 13.1.0 构建通过，生成 `upper_computer/build/ESPWatchUpperComputer.exe`。
- 实测：用户已完成烧录和首次联调，验证热点连接、USB 配置、TCP 认证、息屏断线及唤醒重连。
- 下一步：阶段 7 在该通信基线之上实现电脑协同专注模式与本地历史。

### 2026-09-13 / 阶段 6 首次实测修正

- 用户首次烧录后反馈手表持续重启，阶段 6 保持“待实测”，未作验收。
- 只读检查发现原校时任务与新增连接任务存在启动时并发初始化 Wi-Fi 的窗口；改为 `app_main` 在创建后台任务前单次初始化 Wi-Fi，并让上位机连接任务延后 6 秒进入连接循环。
- 修正版仍需重新构建、烧录并观察；若启动 Wi-Fi 后仍发生重启，需结合串口启动日志判断 Brownout、看门狗或断言，并优先复查此前出现异常的电池/3.3V 供电。
- 用户烧录启动竞争修正版后，上位机成功显示手表 `192.168.137.131` 连接并通过设备 ID/令牌认证，持续闪屏现象消失。
- 息屏实测发现后台连接任务会在屏幕保持关闭时重新启动 Wi-Fi。修正为公共 Wi-Fi 层持有系统休眠暂停标志：息屏停止网络并拒绝所有后台启动请求，唤醒后解除暂停，由连接任务恢复。
- 用户重新烧录后确认最终测试成功：亮屏时手表可连接上位机，息屏后保持断线且不会在黑屏期间自行重连，唤醒后自动恢复连接。阶段 6 据此标记为已验收。
- 2026-09-18 修正插电息屏的电源键失效：TP4056 报告正在充电时不进入 ESP light sleep，改为关闭屏幕后低频轮询 KEY4/抬腕；其他情况下的 light sleep 每 200 ms 定时回退检查一次按键，覆盖充满电后 `CHRG` 无法识别插线及 GPIO 唤醒偶发丢失。长按仍优先释放电源自锁；若 USB 使主控没有真正掉电，则黑屏待机并在再次按 KEY4 时软件重启，不再永久停入不可唤醒循环。
- 2026-09-18 抬腕检测由 BMI270 25 Hz data-ready 唤醒改为约 5 Hz 定时采样，减少息屏 CPU/I2C 活动和串口日志；判定沿用手势识别的状态确认思想，要求明确运动后在约 2.4 秒窗口内转入面向用户姿态，并连续两帧稳定，移除单独 `Y<−80 mg` 和宽泛姿态兜底触发。完整 100 Hz 陀螺仪手势任务仍只用于演示页，避免息屏功耗上升。
- 2026-09-18 修复计时器与协同番茄钟缺字：固定入口由缺少“设置”字形的“时间设置”收敛为“时间”；20 px 中文字体建立无环回退链以复用已有字形，电脑任务名显示前逐个验证字形，未内置字符以 `?` 安全替代，避免 LVGL 缺字符号方框。由于应用分区仅余约 180 KiB，不引入超过 1 MiB 的完整 CJK 字库。

### 2026-09-09 / 阶段 1：设备信息

- 状态：已验收；当前改动尚待用户提交，基于本地基线提交 `890a8f9`。
- 完成：主菜单“电子名片/E-Card”改为“设备信息/Device Info”；页面显示拥有者、设备名称及只读设备编号，KEY2 返回。
- 存储：新增独立 NVS 命名空间 `device_info`。设备编号由芯片 Wi-Fi Station MAC 生成并保持稳定；拥有者与设备名通过既有 USB JSON 的 `set_config` 写入，`get_config` 可读取。
- 边界：拥有者和设备名目前最多 32 个 ASCII 字符；固定中文标签使用按需生成的小字库，避免完整中文字库挤占应用分区。具体配置示例见 `DEVICE_INFO_CONFIG.md`。
- 清理：旧 B 站统计任务及电子名片二维码、统计图片不再参与固件构建；源文件仍保留，原 Wi-Fi、天气及图片配置接口未删除。
- 构建：ESP-IDF 5.2.7，`idf.py build` 通过；`ESP32-S3-WATCH-Code.bin` 为 2,624,976 字节，3 MiB 应用分区剩余 520,752 字节（17%）。SHA-256：`51CC78BA5B3FAAD6574C5EFBA20908ADF419C2DF2784B75805984B951ACBBD56`。
- 实测：用户烧录后确认页面未发现问题；通过 COM18 的 VS Code Serial Monitor 成功发送 JSON 并写入设备信息。期间两次 COM18 断连未复现为确定的固件重启，用户判断可能为连接不稳定并同意阶段 1 验收。
- 已知限制：没有逐项留下空值、超长输入、断电持久化及全部旧功能回归的独立日志证据；后续若发现异常，回到阶段 1 提交修复。
- 下一步：用户建立阶段 1 本地 Git 提交；阶段 2 从包含该提交的当前源码开始。

### 2026-09-09 / 阶段 2：保存的时钟

- 状态：编译通过待实测；基于阶段 1 提交 `fb69304`，当前改动未提交。
- 完成：番茄钟顶部在“时间设置”右侧新增“保存的时钟”；三个固定槽位可新增、修改、删除和一键启动，第一版不设置预设名称。
- 交互：预设范围与原时间设置一致，为 00:00:01～99:59:59；空槽按下进入新增；删除确认默认选中取消；启动后回到番茄钟主页面并立即按倒计时运行。运行或暂停中的计时禁止被预设覆盖，提示“请先清零”。
- 存储：新增独立 NVS 命名空间 `tomato_presets`，每个槽位保存有效标记和总秒数；仅新增、修改和删除时提交 NVS，计时过程不写 Flash。槽位删除后不移动其他槽位。
- 字体：新增 20 px 按需中文字库，只包含本阶段界面字符并回退到 Montserrat 数字/英文，控制应用分区占用。
- 涉及文件：`main/watch_tomato_clock.c`、`main/watch_tomato_presets.c/.h`、`main/fonts/tomato_preset_font_20.c`、`main/CMakeLists.txt`、本文。
- 构建：ESP-IDF 5.2.7，`idf.py build` 通过；`ESP32-S3-WATCH-Code.bin` 为 2,636,752 字节，3 MiB 应用分区剩余 508,976 字节（16%）。SHA-256：`3A03254F5B7CD8694FF81EA9482362A5AE048405E4D1AF5A3272498D156DEBDE`。
- 实物测试：尚未烧录；需要验证 0/1/3 个槽位、第四个槽位不可出现、修改/删除取消、掉电保持、一键启动、运行中禁止替换，以及原手动设置/正计时/倒计时。
- 下一步：用户在 VS Code 选择实际手表串口后烧录并按实测清单验收；发现问题先留在阶段 2 修正，验收后再提交本阶段。

### 2026-09-09 / 阶段 2 验收与阶段 3 设计启动

- 阶段 2 已由用户确认完成；当前 Git 提交为 `0389c68`，工作区在阶段 3 设计前干净。
- 阶段 3 先完成游戏选型，不实施代码。新增 `STAGE3_GAME_DESIGN.md`，比较贪吃蛇、方向反应和三键序列记忆。
- 推荐首个新增游戏为“贪吃蛇”：上拨左转、下拨右转、按下暂停；不依赖新增 KEY2 长按，对已有页面影响较小。
- 待用户决定游戏类型，以及贪吃蛇是否保存一个历史最高分。

### 2026-09-09 / 阶段 3 范围与内存约束补充

- 用户明确只加入一个游戏；阶段 3 及当前总计划不继续安排第二个新增游戏。
- 用户要求注意应用内存。设计同时约束 Flash 固件容量和运行 RAM；当前 `sdkconfig` 未启用 PSRAM，不能依赖硬件上的 8 MB PSRAM。
- 推荐贪吃蛇采用单个 LVGL 棋盘对象自绘和固定坐标数组，不创建逐格/逐蛇段对象，不使用棋盘 RGB565 Canvas，不新增 FreeRTOS 任务。
- 固件增量目标不超过 60 KiB，并尽量保留至少 400 KiB 应用分区余量；运行时页面堆增量目标不超过 8 KiB。最终以构建及真机内存趋势记录为准。

### 2026-09-09 / 阶段 3 贪吃蛇实现与构建

- 用户确认新增游戏为“贪吃蛇”，保存历史最高分，并要求重点控制应用内存。
- 新增 `watch_snake.c/.h`：12×12 棋盘、自动移动、相对左/右转、食物、增长、加速、撞墙/自撞、暂停/继续、主动结束、失败/胜利、重玩/返回和最高分 NVS。
- 游戏中心改为“返回 → 飞机大战 → 贪吃蛇”三项循环选择；原飞机大战模块及其分数存储未改动。
- 采用单个 LVGL 自绘棋盘、固定 144 字节蛇身数组和一个 LVGL timer；不使用 Canvas、不随蛇增长分配、不新增 FreeRTOS 任务。中文使用只包含本游戏所需字符的 `snake_font_20`。
- 最高分保存在独立 `snake_score/best` NVS 项中；仅在失败、胜利或用户主动结束且刷新纪录时写入。
- ESP-IDF 5.2.7 构建通过，无新增编译警告。菜单字号修正后的固件为 2,647,056 字节，相对阶段 2 的 2,636,752 字节增加 10,304 字节；3 MiB 应用分区剩余 498,672 字节（16%）。固件 SHA-256：`191FF0693FFA8F8CE190ADD60759D2CAF2D72205505CB0A91DF387826BAB30C1`。
- 根据用户实测反馈前的界面要求，将游戏中心“贪吃蛇”菜单项由 20px 调整为与“飞机大战”相同的 26px；新增字库仅含“贪吃蛇”三个汉字，固件增加 1,056 字节，游戏内部字号和逻辑未改。
- ELF 中静态 `s_snake` 上下文为 292 字节；实际 LVGL 页面堆增量和退出后的内存趋势仍需真机日志验证。
- 阶段 3 后续已提交为 `1ad33b0`。本计划尚无实物验收记录：仍需补记菜单与布局、转向和重复输入过滤、暂停/结束/重玩、碰撞与计分、重启后最高分、飞机大战回归，以及连续进出 20 次的堆内存趋势。

### 2026-09-09 / 阶段 4 指南针界面与指北箭头

- 状态：编译通过待实测；基于阶段 3 提交 `1ad33b0`。阶段 3 已存在源码提交，但本计划没有找到对应的实物验收记录，后续仍需补记。
- 完成：保留原有旋转刻度盘和磁力计航向计算，增加中央指北箭头。红色箭头始终指向真实北方，灰色短尾表示相反方向；屏幕顶部金色标记固定表示手表朝向参考。
- 界面：改为深蓝背景、青色双环和分级刻度，北方标识使用红色；航向数字移至圆盘下部的独立信息框，给阶段 5 的调平提示预留空间。
- 稳定性：新增基于单位向量的圆周指数平滑，正确处理 359°/0° 跨界；传感器未初始化、溢出、读取失败或校准数据异常时隐藏指北箭头并显示明确英文状态，恢复后从新读数重新建立平滑状态。
- 阶段边界：本阶段没有修改 QMC5883P 校准参数、磁力计航向公式、BMI270 接口或加入倾斜补偿；这些仍属于阶段 5，不能把本阶段称为已具备倾斜补偿。
- 涉及文件：`main/watch_compass.c`、本文；无需增加源文件或修改 `main/CMakeLists.txt`。
- 构建：ESP-IDF 5.2.7，`idf.py build` 通过，指南针改动无编译警告；`ESP32-S3-WATCH-Code.bin` 为 2,648,368 字节，3 MiB 应用分区剩余 497,360 字节（16%）。SHA-256：`DF6BBD65D9328864056B31B4A624F313956DB5D11B3C7B6639162328202CED5B`。
- 实物测试：尚未烧录。需水平放置并缓慢旋转一周，检查红色箭头、N 刻度和航向数字关系；重点跨越 359°/0°、观察静止抖动、文字和箭头重叠、反复进出页面，以及原返回操作。传感器故障提示暂不要求人为拆机验证。
- 下一步：用户在 VS Code 中选择实际手表串口后烧录并完成上述实测；若方向相反或存在固定角度偏差，先记录表的实际朝向和读数，再区分坐标映射、安装偏移与磁校准问题。

### 2026-09-09 / 阶段 5 共享采样、调平与倾斜补偿

- 状态：已验收；阶段实现提交 `3338dd9`，实物轴向修正提交 `d54bf49`，左右补偿轴修正提交 `dbe46df`。
- 共享采样：`watch_bmi270` 新增带 `timestamp_us`、mg 单位和 `valid` 状态的三轴加速度接口。继续使用原 25 Hz、±4g、仅加速度计模式；不打开陀螺仪、不新增任务，也不改变抬腕检测的进入/退出配置。
- 算法：磁场和重力先映射到同一板级坐标，再将设备前向与第二轴水平化并正交化，最后投影三轴磁场计算航向。该方法保留水平时原 XY 航向语义，并避开单纯投影磁场后直接取器件 X/Y 的倾斜误差。
- 有效性：加速度模长须在 750～1250 mg，连续两帧稳定后才输出方向；帧间三轴变化和超过 65° 的姿态分别显示 `MOVING`、`TOO STEEP`。中心点作为水平气泡，8° 内显示 `LEVEL`。校准后用磁场总强度基线识别明显突变并显示 `MAG FIELD`，但不能识别所有磁干扰。
- 校准：上拨启动 15 秒三轴磁校准，需绕所有轴旋转；下拨取消且保留原参数。覆盖不足不保存；成功后把 XYZ 偏置及比例写入独立 NVS 命名空间 `compass_cal`。未校准时明确显示 `CAL FIRST` 并保留原二维水平指南针，避免使用未校准 Z 轴做倾斜补偿。
- 板级轴向：最初依据 PCB 封装旋转角作出的同轴假设已被真机数据修正。当前成品五姿态日志确认 BMI270 的屏幕顶部、屏幕右侧和屏幕朝外分别对应原始 Y 正、X 正和 Z 正。首轮补偿实测进一步确认：前后轴使用 Y 正时误差小于 1°，左右轴必须以 `-X` 对齐 QMC5883P 原二维航向的第二轴；水平气泡单独转换回屏幕坐标。固定地磁偏角 `-3.2°`、安装偏移 `15°` 和首轮运动阈值仍待后续实测。
- 涉及文件：`main/watch_bmi270.c/.h`、`main/qmc5883p.c/.h`、`main/watch_compass.c`、新增 `main/watch_compass_calibration.c/.h`、`main/CMakeLists.txt`、`tests/test_qmc5883p_tilt.c`、`STAGE5_COMPASS_TEST.md`、本文。
- 逻辑测试：使用本机 MinGW 以 `-Wall -Wextra -Werror` 编译独立测试，覆盖水平 0°/90°、前后 ±30°、左右 ±30°、359° 及零重力/磁场与重力平行的退化向量，结果通过。
- 构建：左右补偿轴修正后用 ESP-IDF 5.2.7 执行 `idf.py build` 通过且无新增编译警告；`ESP32-S3-WATCH-Code.bin` 为 2,654,448 字节，相对阶段 4 增加 6,080 字节，3 MiB 应用分区剩余 491,280 字节（16%）。SHA-256：`156A6F07F88E9B3F6D154E3BEFE57BBB8E58A3222787A7458A2AF614B7DD23B2`。静态指南针上下文 `s_compass` 为 1,468 字节。
- 实物测试：用户已用当前成品完成五姿态轴向记录并成功保存三轴磁校准，日志重进页面后保持 `cal=1`。同一朝向下，水平读数约 14.8°～16.6°，顶部抬高约 30° 后约 15.5°～16.9°，证明前后倾斜补偿有效；右侧抬高约 29° 时错误跳到约 66.7°～67.4°，据此修正左右补偿轴符号。用户随后确认当前结果可以提交并接受阶段 5。左右修正版没有单独留下复测日志；若后续发现左右倾斜误差、气泡方向或运动提示异常，回到 `dbe46df` 后续修复。
- 下一步：阶段 6 开始前先检查上位机压缩包、现有 Wi-Fi 生命周期和可复用通信接口；阶段 5 后续仅处理实测发现的问题。

### 2026-09-13 / 阶段 8：WPS 演示遥控实现与构建

- 状态：已验收；基于阶段 6 提交 `478dcfa`，当前改动未提交。用户决定跳过阶段 7，直接实施阶段 8。
- 手表端：主菜单增加“演示遥控”和代码绘制的放映图标；页面显示连接、电脑端开关、阶段 9 手势占位及最近操作结果。上拨上一页、下拨下一页；KEY2 短按只提示手势将在阶段 9 启用，长按 1 秒退出，长按不会触发短按。
- 单次输入：按键驱动补充 KEY1/KEY3 释放事件，演示页在一次按住期间锁存，屏蔽原有 450 ms 后的重复事件，因此每次拨动最多发送一个翻页请求；其他页面继续沿用原按键按下和重复行为。
- 通信：公共网络任务新增 8 项有界发送队列。演示页活动时网络轮询周期为 20 ms，空闲为 250 ms；请求包含会话、递增 `op_id`、方向和 1500 ms 有效期。断线清空瞬时请求，重连只同步页面状态，不补发翻页；手表 UI 通过线程安全快照读取电脑开关及处理结果。
- 上位机：占位页替换为演示控制页，开关在每个新连接中默认关闭。按 `session + op_id` 缓存最近 64 个结果并去重；只有电脑开关已开、手表页面活动、会话匹配且前台为 WPS/PowerPoint 全屏放映窗口时，才通过 Windows `SendInput` 模拟 `Page Up` / `Page Down`。
- WPS 兼容：本机 WPS 12.1.0.28505 的系统演示入口实际指向统一进程 `wps.exe`，因此同时接受新版 `wps.exe`、旧版 `wpp.exe` 和 `powerpnt.exe`。全屏窗口判断仍需真机放映确认；点击上位机开关后必须切回 WPS 放映窗口。
- 电源：演示遥控页可见及进入动画期间抑制 15 秒自动息屏，保证拨轮无线控制连续可用；KEY4 主动息屏和长按关机行为保持不变，主动息屏仍停止 Wi-Fi 和演示控制。
- 字体与容量：新增仅含阶段 8 中文字符的 20 px 字库。首次实测发现精简字库没有全角冒号，状态行显示缺字方框；修正版移除这类标点，缩短状态和操作文案，增加分隔线并重新分配纵向间距。第二次实测又发现“下一页”末字换行、底部“退出”末字被裁切，进一步把两行分别压缩为“上拨上一页 下拨下一页”和“短按手势说明 长按退出”，底部说明从 y=211 上移到 y=202。ESP-IDF 5.2.7 增量构建通过；当前固件 `0x29bc30`（2,735,152）字节，3 MiB 应用分区剩余 `0x643d0`（410,576）字节，13%；SHA-256 为 `FEBFDA21A79D03C507B389812C421C6372E3153F186B233236C67A673FA9A24D`。
- 电脑构建：Qt 6.11.2 + MinGW 13.1.0 构建通过，生成 `upper_computer/build/ESPWatchUpperComputer.exe`；隐藏启动冒烟测试 3 秒通过。
- 实物测试：用户确认手表与 WPS 的基本连接和翻页功能通过。首版照片暴露全角冒号缺字方框和底部文案换行拥挤；第一次修正后仍有“下一页”末字换行及“退出”末字被裁切，第二次压缩文案并上移底部说明后，用户于 2026-09-13 确认通过并同意阶段提交。电脑“已处理”只代表模拟按键已发送，真实画面变化由本次现场观察确认。
- 验收边界：本次确认覆盖基本 WPS 翻页和最终页面显示；没有逐项留下按住不连翻、禁用/离页/非前台/断线、长短按和息屏等异常项的独立测试记录，后续若发现问题回到阶段 8 修正。
- 下一步：由用户提交阶段 8 源码和文档；阶段 9 从该提交开始实现左右挥手识别与防误触。

### 2026-09-13：阶段 9A 独立采样交接（待实测）

- 基线：`master` / `a52e1b7`（阶段 8）。本次改动未提交，未烧录；阶段 7 继续跳过。
- 用户决定：左手佩戴、表盘朝外；先独立采样，暂不修改 Qt 上位机。阶段 9 拆为 9A 采集与数据确认、9B 识别与阶段 8 接口集成。
- 实现：`watch_bmi270.c/.h` 添加六轴采集会话，保存/恢复原配置，临时启用 100 Hz 加速度与陀螺仪；`watch_imu_capture.c/.h` 在 RAM 中记录有限时长，采完恢复后导出；`watch_serial_config.c` 新增独立 USB `imu_capture` 命令；`main.c` 防止采集中自动息屏，KEY4 先取消和清理再执行原电源行为。同步 `main/CMakeLists.txt`。
- 独立工具：`tools/capture_imu.py` 保存原始值、单位换算、时间戳和 JSON 质量记录，验证序号、数量、时间和恢复结果。失败保留 `.partial.csv`。`captures/` 已加入 `.gitignore`；不写入手表 Flash，不更改无线协议或 `upper_computer/`。
- 验证：ESP-IDF 5.2.7 在现有 `build/` 编译通过，新增 C 代码无编译警告；环境仍提示缺少可忽略的 `idf_version.txt`。固件 `0x29c5d0`（2,737,616）字节，3 MiB 应用分区剩余 `0x63a30`（408,112）字节，13%。SHA-256：`0961E4282B3013942142C5884F39E36A001B7CC02AD09AEB3554C881C71F8AEF`。构建后仅调整模块说明注释，未改变可执行逻辑。
- 工具验证：`python -m unittest discover -s tests -p test_capture_imu.py` 六项通过，包含单位换算、序号/时间/范围校验、质量提示、失败状态以及模拟完整导出/丢帧的文件结果。`git diff --check` 通过。
- 实物状态：尚未采集真实数据；KEY4 取消、采集后指南针/抬腕恢复、有效采样率均待验证。轮询采样采用实际读取时间戳，不能承诺严格等间隔或无硬件丢样；先检查质量再调阈值。USB 线可能影响动作，需留松弛余量。
- 首轮采集反馈：两次传感器记录均完成且恢复结果为 0，其中一组记录 585 帧、传感器读取错误为 0；但无节流批量导出分别在电脑收到 243 帧和 175 帧后出现发送取消/序号缺帧，因此不能作为合格样本。已在每个成功导出帧后让出一个 10 ms 系统节拍，避免 4 KiB USB TX 缓冲区先接收后丢帧；完整性校验仍保留。修正后 ESP-IDF 增量构建和六项工具测试通过，固件大小仍为 `0x29c5d0`，SHA-256 为 `E79DD4BA37E2030839B077B15EA7B797372265B47557425AA01FB5EECDAAF15A`；待重新烧录实测。
- 节流修正实测：三组 6 秒采集均完整保存，采样率 97.7～98.0 Hz、最大间隔 20.1～20.4 ms、恢复结果为 0。第一组含明显移动（重力模长约 921 mg，加速度标准差最高约 269 mg），不作为静止基准；后两组重力模长均约 990 mg，加速度各轴标准差约 2.9～6.3 mg，陀螺仪各轴标准差约 0.29～1.10°/s，可作为胸前静止基准。下一步先左右各采 3 组完整动作，确认方向波形后再批量采集。
- 左右试采：左挥和右挥各 3 组均完整，采样率 96.9～98.0 Hz，最大间隔约 20.4 ms。当前样本中方向主要由陀螺仪 Y 轴区分：左挥负峰为 −278～−353°/s，右挥正峰为 +313～+418°/s；候选阈值 −250/+280°/s 在对应各组出现 8～15 帧，六组暂未互相误判。样本量尚不足以固定阈值，先采只抬手、自然转腕等干扰动作，检查误触边界后再扩大正样本。
- 干扰试采：只抬手 3 组的陀螺仪 Y 轴范围为 −72～+71°/s，自然转腕 3 组为 −90～+166°/s，均未越过候选方向阈值。转腕的总角速度峰值可达约 623°/s，证明不能用总运动强度单独触发；应组合方向轴峰值速度、同向持续时间/角度累计作为动作幅度代理，并用准备姿态和触发后锁定约束。BMI270 不直接测量厘米距离，加速度二次积分漂移大，首版不向用户宣称真实距离测量。新增慢速全幅和快速短幅标签，分别确定最低速度与最低有效幅度。
- 速度/幅度边界试采：慢速全幅左右各 3 组、快速短幅左右各 3 组均完整。有效左右动作的方向峰值最低约 278°/s，短幅最高约 211°/s，慢速全幅最高约 63°/s；方向速度超过 100°/s 部分的全段累计量，有效动作最低约 22.3°，短幅最高约 6.7°，慢速为 0。当前默认候选为方向峰值至少约 240°/s且同一次动作累计有效幅度至少约 15°；阈值尚未固化，继续用讲话、走动和更多有效动作回放验证。
- 讲话/走动回放：讲话和原地走动各 3 组、每组 10 秒均完整。仅用全段速度与累计量会把讲话第 1 组中的一次大动作误判为右挥；该动作开始时加速度 X 约为 −900 mg，而胸前准备姿态及有效动作开始前约为正向，加入“胸前姿态连续停稳后才武装、离开姿态后仅保留短暂启动窗口”后，所有静止和干扰样本均无触发。离线状态机识别当前 3 组右挥和 2 组规范左挥；另 2 组左挥在抬手后没有形成至少约 0.15 秒稳定段便直接横挥，按既定流程拒绝。手表端应显示就绪状态，帮助用户在真正武装后动作；需补采明确停稳的左挥确认可重复性。
- 阶段 9B 实现：新增 `watch_gesture_recognizer.c/.h` 纯状态机和 `watch_gesture.c/.h` 100 Hz 采样任务/事件队列；识别仅在用户于演示遥控页短按开启后运行，LVGL 定时器从队列取左右事件并复用阶段 8 翻页发送接口。页面以“等待/已开启/暂未启用”显示准备状态；触发后锁定至放手，退出、长按、KEY4 息屏或关机时停止任务并恢复 BMI270 配置。上位机与无线协议未改。
- 阶段 9B 验证：Python 离线回放模型拒绝全部已采静止、抬手、转腕、慢速、短幅、讲话和走动样本；识别全部 3 组右挥及 2 组符合停稳流程的左挥，并按设计拒绝 2 组未停稳便横挥的左挥。九项 Python 协议/状态机测试通过，`git diff --check` 通过。ESP-IDF 5.2.7 构建通过，固件 `0x29cf20`（2,740,000）字节，3 MiB 应用分区剩余 `0x630e0`（405,728）字节，13%；SHA-256 为 `A37E519818F0B2C42BE99CB6363490306A8E439179FBF3C7FD992FED41EAEB14`。尚未烧录本版，页面状态、真实翻页、单次触发和传感器恢复均待实测；详见 `STAGE9_GESTURE.md`。
- 阶段 9 实物验收：用户于 2026-09-13 烧录后确认“通过了”，覆盖演示遥控中的手势启用/准备状态和实际左右翻页基本流程，阶段 9 据此标记已验收。没有分别留下 10 次正确率、1 分钟讲话/走动误触、断线不补发、KEY4 后恢复和指南针/抬腕回归等每个扩展检查项的独立记录；这些量化与整体回归放到阶段 10，若提前发现问题则回到阶段 9 小范围调整。
- 版本状态：阶段 9 工作区尚未提交，基线仍为 `a52e1b7`；由用户按项目约定自行提交。下一步为阶段 10 整体回归、识别率/误触率/响应时间统计与展示交付；阶段 7 仍保留为后续实现。
- 下一步：按 `STAGE9_CAPTURE.md` 烧录后先采一组 6 秒 `still`，确认数据和恢复行为，再采左右及干扰样本。识别算法、阈值和上位机手势配置留到数据分析后实现，阶段 9 尚未验收。

### 2026-09-15 / 阶段 7：电脑协同专注与本地历史

- 状态：代码已实现，ESP-IDF 与 Qt 均编译通过，尚未烧录和实机验收；当前分支 `wyx_branch`，基线 `203fcea`，改动未提交。
- 手表端：主菜单把原本地正/倒计时改名为“计时器”，保留三个本地预设和全部原交互；其后新增“番茄钟”，直接进入 240×240 电脑协同页。协同页显示连接、项目、剩余时间、计划和状态；上下拨选择主操作/结束，按下执行，结束默认选中取消。离页不结束电脑任务；断线显示重试/返回并禁用任务控制，重连请求完整快照。
- 通信：复用阶段 6 的认证、会话和心跳。新增页面状态、快照同步及开始/暂停/继续/中止消息；手表瞬时请求断线不补发，电脑按会话和最近 64 个 `op_id` 去重。快照含任务 ID 与递增版本；手表只应用非旧版本，并在运行快照间用 FreeRTOS 单调时钟插值，到零仍等待电脑确认。
- 上位机：专注占位页替换为任务面板，支持中文项目名、1～720 分钟、单一当前任务和两端操作。有效专注时间由 `QElapsedTimer` 计算，暂停不累计；睡眠/锁屏期间按单调时间继续累计。完成/中止记录原子写入应用数据目录 `focus_history.json` 并按任务 ID 去重；运行或暂停时关闭窗口默认取消，确认后中止并记录。异常退出不恢复活动任务，也不伪造记录。
- 电源：协同页不抑制原有自动息屏；电脑在手表睡眠断线期间继续计时，唤醒重连后恢复权威状态。KEY4 行为未改。
- UI/素材调整：用户提供的 `SuCai/clockicon.bmp` 经可重复脚本缩放为透明背景 120×120 ARGB 图标，供“计时器”使用；原番茄图标移给新的协同入口。“计时器”中文标题渲染为小型透明位图，绕开精简字库缺少“器”字的问题。协同页按按钮内容切换已有精简字体，修复“重试”等文字使用错误字库导致的方框乱码。
- 协同页交互细化：电脑无活动任务时移除无意义的 `PC` 操作，只显示居中的“返回”；运行/暂停操作改为暂停/播放符号。中途结束改用独立的“结束专注？”确认层，左右焦点限定在“取消”“确认”两个按钮内并默认选择取消；固定中文标题使用可重复生成的透明位图，避免精简字体缺字。
- 字体边界：Qt 端完整显示 UTF-8 中文项目名；固件现有中文字体均为按界面固定字符裁剪，不能承诺任意汉字项目名。手表仍接收并安全截断 UTF-8 项目名，但未包含的汉字可能显示缺字方框；固定按钮和菜单标题已不再依赖缺失字形。
- 构建：ESP-IDF 5.2.7 构建通过，固件 2,832,800 字节（`0x2b39a0`），3 MiB 应用分区剩余 312,928 字节（10%），SHA-256 `F9EB8607EAF51A71680D4136BAACDDBFD77CCB105FB1C6804427BE88FB9C336C`。Qt 6.11.2 + MinGW 13.1.0 构建通过，产物 `upper_computer/build/ESPWatchUpperComputer.exe`。
- 待实测：两端开始/暂停/继续同步、暂停时间排除、正常到零、中途结束只记录一次、并发按键确定性、断线/重连、离页返回、关闭窗口取消、息屏唤醒，以及 240×240 布局和目标中文名显示。通过前不得标记阶段 7 已验收。
- 下一步：烧录当前固件并运行新上位机，先用 1 分钟任务完成一轮端到端测试，再验证 25 分钟任务创建与暂停/重连。

### 2026-09-15 / 后台计步核心（待实机验证）

- 硬件条件：现有 BMI270 内置针对腕戴场景优化的硬件计步器，因此无需增加传感器。计步复用现有 IMU 和 I2C，但不复用阶段 9 的挥手阈值算法；后者只在演示页临时运行 100 Hz 六轴任务，不适合全天计步。
- 实现：新增 `watch_steps` 后台服务和纯逻辑 `watch_step_accumulator`。系统启动后启用 BMI270 Step Counter，每秒读取一次；熄屏/ESP32 light sleep 时暂停 I2C 读取但保持传感器内部计数，唤醒后补齐增量。服务提供线程安全快照，供后续 LVGL 页面读取。
- 持久化：NVS 保存日期、今日总数和传感器基线；每增加 100 步或距上次保存 5 分钟后写入，关机前强制保存。增加固定容量的 31 天日期—步数历史，按日期从新到旧保存，跨越未开机日期时自动补零；首次升级按需求初始化 2026-09-08～09-14 为 0。支持普通重启、传感器内部计数复位和联网/RTC 校时后的自然日切换。
- UI：主菜单在“番茄钟”后增加“计步器”，图标由 `SuCai/SPORT.bmp` 生成 120×120 透明 ARGB 位图。今日页显示实时步数和“历史步数”按钮；历史页一次显示 8 行，白框表示焦点，上拨首行回到返回键，向下越过可视底行时逐行滚动，越过最旧记录后回到最新记录，按下普通记录不执行动作。
- 文字资源：为“计步器”“今日步数：”“历史步数”生成小型 A8 透明度中文字图，避免现有裁剪字体缺字，并比 ARGB8888 文字图减少约四分之三空间；生成过程已加入 `tools/generate_menu_assets.ps1`，数字继续复用 Montserrat 字体。
- 调试：USB 配置通道增加 `{"cmd":"get_steps"}`，新增 `tools/query_steps.py` 和 `STEP_COUNTER_TEST.md`，可以读取今日步数、原始计数、日期、运行状态和错误码。
- 验证：步数累加器宿主机单元测试以 `-Wall -Wextra -Werror` 编译并通过，覆盖启动恢复、正常增量、计数器复位、无变化和跨日清零。页面、31 天存储与图片完整接入后，ESP-IDF 5.2.7 构建通过。首轮烧录在启动首页时出现 `spinlock_acquire` 断言并循环重启；回溯落在 `watch_face` 的 `time()`，屏幕上的“演示遥控”只是重启前未刷新掉的残影。针对新增历史/NVS造成的计步任务栈压力，将 256 字节历史工作副本移到静态区，任务栈从 3072 增至 4096，并增加启动栈余量日志；修正版构建通过，固件为 2,957,328 字节（`0x2d2010`），3 MiB 应用分区剩余 188,400 字节（6%），SHA-256 `DDE4922D2F717BF611B8CBAC2B4A46C6754201B68DA490307F240817FBCCFF3B`。崩溃修复仍需重新烧录确认。
- 验收边界：后台启动日志已由用户真机确认；新页面与历史存储版本尚未烧录，实际佩戴精度、熄屏累计、重启恢复、31 天翻页和 240×240 布局仍待验证。首轮按 `STEP_COUNTER_TEST.md` 检查页面和自然走路 100 步，再根据误差决定是否需要软件补偿。

## 7. 技术参考

- 原项目：https://gitee.com/huohe-laboratory/esp32-multifunctional-watch
- Bosch BMI270 官方产品及文档入口：https://www.bosch-sensortec.com/en/products/motion-sensors/imus/bmi270 （加速度/角速度能力依据；左右挥手仍需本项目自行设计和验证）
- Bosch BMI270 官方 Sensor API：https://github.com/boschsensortec/BMI270_SensorAPI （硬件 Step Counter 的功能页配置和读取方式依据；核对版本 v2.113.0、提交 `41129fcfe39c583ee5462d79195741945d51c1fe`）
- QST QMC5883P Rev.E 数据手册：https://www.qstcorp.com/upload/pdf/202512/2C939E5AA0704285BC3BE71132B8629B.pdf （三轴磁场、量程、封装方向和寄存器依据）
- Qt WebSockets 官方文档：https://doc.qt.io/qt-6/qtwebsockets-index.html （支持双向客户端/服务端；仅为当前建议，采用版本在阶段 6 核实）
