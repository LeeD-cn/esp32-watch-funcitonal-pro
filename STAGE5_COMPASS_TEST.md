# 阶段 5 指南针校准与实测清单

本阶段的轴映射、运动阈值和倾斜补偿必须用当前成品验证。编译通过只能说明代码可生成固件，不能证明传感器方向或航向精度正确。

## 1. 烧录与首次进入

1. 在 VS Code 中确认串口仍是当前手表端口，再正常烧录；不要执行 Erase Flash。
2. 打开串口监视器，进入指南针页面。
3. 首次没有保存过磁校准时，底部应显示 `CAL FIRST | UP:CAL`。此时水平二维指南针仍可用，倾斜补偿尚未启用。
4. 中心小点是水平气泡：绿色 `LEVEL` 表示倾角不超过 8°；青色 `TILT n` 表示正在补偿；快速移动显示 `MOVING`；倾角超过 65° 显示 `TOO STEEP`。

## 2. 确认 BMI270 轴方向

串口每约 1 秒输出一行：

```text
mag_raw=x/y/z acc_raw=x/y/z tilt=... state=... cal=... heading=...
```

将手表远离扬声器、磁吸表带、电脑电源和大块金属，静止保持下列姿态并记录 `acc_raw`。下表轴向已由 2026-09-09 的当前成品五姿态日志确认：

| 姿态 | 预期主要变化 |
|---|---|
| 屏幕水平朝上 | Z 约为 +1000 mg，X/Y 接近 0 |
| 抬高手表顶部 | Y 变为明显正值 |
| 抬高手表底部 | Y 变为明显负值 |
| 抬高屏幕右侧 | X 变为明显正值 |
| 抬高屏幕左侧 | X 变为明显负值 |

当前板级映射采用“屏幕顶部 = 原始 Y、屏幕右侧 = 原始 X、屏幕朝外 = 原始 Z”。如果换用另一块硬件后符号或轴不一致，应先记录五种姿态的三轴数值并停止判断补偿精度；只需调整 `watch_compass.c` 顶部的板级映射宏，无需改公式。

## 3. 完成三轴磁校准

1. 建议摘下手表；进入指南针后上拨一次，出现 `CAL 00%`。
2. 在 15 秒内缓慢改变所有方向，至少让屏幕依次朝上、朝下、朝左、朝右、顶部朝上、底部朝上，同时绕各方向转动。避免靠近磁性物体。
3. 下拨可取消，原校准不会被覆盖。
4. 成功时显示 `CAL SAVED`；覆盖不充分时显示 `CAL FAILED`，需重新开始并增加各方向旋转范围。
5. 退出、重新进入或重启后，串口日志中的 `cal=1` 应保持，证明 NVS 数据已载入。

## 4. 调平和倾斜补偿测试

先选一个远离金属的位置，以手机指南针或固定房间方向作参考。手机也应离手表一段距离，避免二者磁性部件互相干扰。

保持手表顶部朝向不变，分别记录：

| 姿态 | 参考角度 | 手表角度 | 与水平读数差值 | 状态 |
|---|---:|---:|---:|---|
| 水平 |  |  | 0° |  |
| 顶部抬高约 15° |  |  |  |  |
| 顶部抬高约 30° |  |  |  |  |
| 底部抬高约 15° |  |  |  |  |
| 底部抬高约 30° |  |  |  |  |
| 左侧抬高约 15° |  |  |  |  |
| 左侧抬高约 30° |  |  |  |  |
| 右侧抬高约 15° |  |  |  |  |
| 右侧抬高约 30° |  |  |  |  |

同一朝向下，倾斜后的读数应明显接近水平读数。最终允许误差要根据这批数据确定，不能直接采用传感器数据手册里的芯片理想精度。

## 5. 异常与旧功能回归

- 快速晃动时应显示 `MOVING` 并隐藏方向箭头；重新静止约 0.4 秒后恢复。
- 接近竖直时应显示 `TOO STEEP`，回到可用倾角后恢复。
- 校准完成并建立环境磁场基准后，靠近明显磁性物体可能显示 `MAG FIELD`；离开干扰源后应恢复。该检测只能发现明显的场强变化，不能识别所有磁干扰。
- 反复进入和退出指南针 20 次，页面、定时器和按键保持正常。
- 退出指南针后测试自动息屏、KEY4 唤醒和抬腕唤醒，确认共享 BMI270 没有改变原低功耗行为。
- 固定地磁偏角 `-3.2°` 和安装偏移 `15°` 仍需用当地参考方向验证；发现固定偏差时先记录数据，不要用重新磁校准掩盖固定角度问题。
